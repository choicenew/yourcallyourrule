# 首页（Home Page）与 UI 架构设计规范与重构总结

本文档汇总整理了此前关于主页（Home）、高级/VIP 模块（Elite/VIP Promotion）、功能中心（Feature Center）及各卡片组件的 UI 架构设计方案与演进规划，供后续设计与功能迭代查阅。

---

## 一、主页整体布局层级架构 (HomePage Hierarchy)

主页采用垂直流式卡片架构（`SingleChildScrollView + Column`），按业务优先级与用户视觉动线从上至下组织：

```
┌────────────────────────────────────────────────────────┐
│                   1. HomeAppBar                        │ (状态指示、快捷设置入口)
├────────────────────────────────────────────────────────┤
│                   2. CarouselCards                     │ (多维轮播卡片：图表/模拟预览)
├────────────────────────────────────────────────────────┤
│                3. RuleVerificationCard                 │ (来电规则即时验证沙盒)
├────────────────────────────────────────────────────────┤
│               4. FilterManagementWidget                │ (多 SIM 卡规则与拦截总控台)
├────────────────────────────────────────────────────────┤
│                   5. FeatureCenter                     │ (24色微拟态功能中心网格)
├────────────────────────────────────────────────────────┤
│                 6. VipPromotionCard                    │ (Elite/VIP 尊享权益推广卡片)
├────────────────────────────────────────────────────────┤
│              7. nativeAdWidgetMedium                   │ (智能自适应广告位)
└────────────────────────────────────────────────────────┘
```

---

## 二、核心卡片组件设计规范与实现细节

### 1. 轮播卡片中心 (`CarouselCards`)
* **架构设计**：采用“通用卡片外壳模板（`_buildCarouselCard`）+ 自定义内容插槽（`customContent`）”设计。
* **卡片类型**：
  1. **拦截统计图表卡片**：嵌入 `StatisticChart`，支持 `Week` / `Month` 时间跨度快速切换与拦截趋势图展示；
  2. **来电悬浮窗预览卡片**：嵌入 `CallerIdOverlayMock`，向用户直观演示来电标记在真实屏幕上的浮窗效果；
  3. **社区防诈规则卡片**：展示云端社区规则库的拦截总次数与贡献指标。
* **动效机制**：内置 `Timer.periodic(Duration(seconds: 5))` 自动平滑轮播（500ms `Curves.easeInOut`），并在卡片少于 1 页或组件销毁时自动清理定时器。

---

### 2. 规则即时验证沙盒 (`RuleVerificationCard`)
* **设计意图**：降低用户配置规则的学习门槛。
* **功能特性**：
  * 提供即时测试输入框，用户输入任意测试号码（如 `95013...` 或带区号固定电话）；
  * 毫秒级反馈：直接显示命中何种规则（黑名单拦截、VIP 放行、插件云查标记等）；
  * 减少用户在真实来电时才发现规则配置错误的隐患。

---

### 3. 多 SIM 卡过滤总控台 (`FilterManagementWidget`)
* **设计意图**：完美适配双卡/多卡用户的独立规则配置。
* **交互细节**：
  * 自动监听 `simCardsProvider`：根据手机实际插卡数量（单卡/双卡）动态生成顶部 Tab 切换器（`TabController`）；
  * 聚合管理：
    * **时间段拦截器**（夜间免打扰、工作时间拦截）；
    * **频次过滤器**（防连环轰炸、陌生人短时多呼限制）；
    * **本地与云端规则开关**。

---

### 4. 24 色高级柔和色相功能中心 (`FeatureCenter`)
* **设计系统（Color Palette System）**：
  内置 24 种各具辨识度的高级柔和色相池，避免刺眼的纯色，营造温润典雅的现代质感：
  * `0xFFE8F5E9` 薄荷浅绿
  * `0xFFE3F2FD` 天空浅蓝
  * `0xFFF3E5F5` 薰衣草紫
  * `0xFFFFF3E0` 温暖浅橙
  * `0xFFE0F7FA` 冰海浅青
  * `0xFFFCE4EC` 柔和浅粉
  * `0xFFFFF8E1` 阳光浅琥珀
  * `0xFFEDE7F6` 优雅深紫罗兰
  * `0xFFE0F2F1` 极光碧绿
  * `0xFFFFEBEE` 珊瑚柔红 等 24 色
* **功能网格与排序**：
  * 支持网格布局（Grid）；
  * 支持长按拖拽自定义排序（`_isReorderEnabled`），排序持久化存储于本地；
  * **20+ 细分功能入口**：标记号码管理、JS 插件管理、黑白名单、SIM 槽规则、云端同步、自动更新、设备管理等；
  * **VIP 门禁守卫（`VipAccessChecker`）**：部分高级功能自动校验会员状态，未开通时平滑引导至购买页。

---

### 5. 高级会员/VIP 推广体系 (`VipPromotionCard` & `ModernPurchasePage`)
* **视觉语言**：
  * 主色调：采用温暖尊贵的琥珀金与现代渐变（`Color(0xFFF5A623)` 至 `Color(0xFFFFF3E0)`）；
  * 拟物光泽感与圆角卡片结合。
* **权益矩阵展示**：
  * 无广告纯净体验；
  * 无限量云端规则双向同步；
  * 高级远程 JS 插件引擎无限制执行；
  * 双卡独立规则极致精细化配置。

---

## 三、UI/UX 性能与加载规范

1. **首帧渲染零阻塞**：
   * 所有重型服务与网络请求均在后台异步执行，首页卡片仅监听本地轻量 Provider，确保启动 0 延迟秒开；
2. **广告组件自适应优雅降级**：
   * 广告加载中或失败时自动折叠为 `SizedBox.shrink()`，绝不残留硬性空白色块，保持视觉流畅完整；
3. **多语言与无障碍支持**：
   * 所有卡片标题与功能文案统一走 `AppLocalizations.of(context)!` 国际化系统。
