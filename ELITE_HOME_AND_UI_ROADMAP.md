# Elite Home 与全局多巴胺 UI 架构设计规范与迭代方案

> **当前独立分支**：`elite-ui`  
> **分支隔离原则**：本分支为独立的 UI 探索与演进分支，完全不影响 `local` 分支。  
> **逻辑安全底线**：严禁篡改底层 `Service`、`Repository`、`Data` 结构及业务 `Provider`，所有业务逻辑仅作为 UI 的只读数据源。

---

## 一、全球化（东西方通用）与多巴胺设计核心原则

面向全球（东亚、北美、欧洲、东南亚等）不同文化与认知习惯的用户，防骚扰与来电管理产品的共性诉求是：**“直觉化、非说教、有即时正向反馈”**。

```
┌────────────────────────────────────────────────────────┐
│               东西方通用的多巴胺心理体验模型              │
│                                                        │
│  [1. 即时安全感] ───> [2. 一级直达过滤] ───> [3. 社区判官]│
│  活力能量环/极速查号    拒接规则一目了然        滑动参与纠错提议 │
│         ▲                                      │       │
│         └──────────────────────────────────────┘       │
│                      [4. 战报与打卡]                    │
│                      省心省时成就与趋势图                │
└────────────────────────────────────────────────────────┘
```

1. **去折叠化（Flatten Hierarchy）**：
   - 传统折叠面板（`ExpansionTile`）对普通用户有“黑盒”恐惧感。核心防骚扰过滤（如拒接模式、高危拦截）提升为**一级直观卡片/开关**，状态一目了然。
2. **渐进式暴露（Progressive Disclosure）**：
   - 常用功能（黑白名单、拦截模式、号码查验、社区投票）直接置于首屏；
   - 极客高级设置（正则调试、JS 插件、卡槽微调、云同步）优雅后置，保留深度而不制造认知负担。
3. **多巴胺视觉语言（Dopamine Visual Language）**：
   - 采用高辨识度柔和渐变与微拟态光晕（绿：安全守护；橙：商业推销；红：诈骗拦截；紫：智能规则）；
   - 微触觉动效与卡片微交互（点赞/投票跳动、滑块吸附）。

---

## 二、Elite 页面全景与一级 UI 架构

```
┌─────────────────────────────────────────────────────────┐
│ 🌟 1. EliteAppBar                                       │
│    - 品牌活力 Icon + 状态微标                             │
│    - [Pro ⇄ Elite] 模式自由切换胶囊                     │
│    - 多语言与主题快捷感知                               │
├─────────────────────────────────────────────────────────┤
│ 🛡️ 2. EliteHeroShieldCard (安全状态与极速查号)           │
│    - 动态呼吸光晕（绿/蓝安全状态）                      │
│    - “今日已守护 X 次通话”                               │
│    - 极速号码查询输入框（自动感知剪贴板）               │
├─────────────────────────────────────────────────────────┤
│ ⚙️ 3. ElitePrimaryFilterCard (一级直达拦截控制中心)      │
│    - 【彻底去折叠】直接在首页提供三大防护档位：          │
│      [🟢 智能防骚扰] [🟡 强力拦截] [🔵 宽松标记]        │
│    - 核心开关一键切换，直观呈现生效中的规则策略        │
├─────────────────────────────────────────────────────────┤
│ 🗳️ 4. EliteCommunityJudgeCard (社区安全判官 C 位卡片)    │
│    - 【核心重点】复用已有 proposalStatisticsProvider    │
│    - 动态展示当前社区热议/争议号码                      │
│    - [赞同删除] / [保持标记] 一键投票微互动             │
├─────────────────────────────────────────────────────────┤
│ 📊 5. EliteStatsInsightCard (多巴胺活力拦截战报)        │
│    - 复用已有 StatisticChart 与拦截趋势数据              │
│    - 活力渐变折线/柱状图 + 拦截类型分布                 │
├─────────────────────────────────────────────────────────┤
│ ⚡ 6. EliteQuickCapsules (多巴胺灵动快捷功能区)          │
│    - 替代原方块九宫格：彩色胶囊 Pill + 场景化大卡片    │
│    - 覆盖所有已有入口：黑白名单、标记、插件、卡槽等     │
└─────────────────────────────────────────────────────────┘
```

---

## 三、各核心模块重构与实现策略

### 1. 一级拦截控制中心（`ElitePrimaryFilterCard`）
* **重构点**：将原本深藏在 `filter_management_widget.dart` 折叠面板中的复杂策略，提炼为**直观的场景化档位卡片**；
* **保留能力**：直接绑定现有的 `callFilterServiceProvider`、`enhancedFilterConfigProvider`，点击直接生效，无需用户进入多层菜单。

### 2. 社区安全判官卡片（`EliteCommunityJudgeCard`）
* **重构点**：提升至首页核心视觉区，做成左右滑动或卡片轮播形态；
* **保留能力**：直接读取 `proposalStatisticsProvider` 和 `deletionProposalServiceProvider`，用户投票即时触发本地与云端逻辑。

### 3. 多巴胺战报图表（`EliteStatsInsightCard`）
* **重构点**：复用 `StatisticChart` 的数据结构，赋予其多巴胺主题色彩与光泽，展示时间维度（日/周/月）的直观战报。

### 4. 灵动功能胶囊（`EliteQuickCapsules`）
* **重构点**：将 `FeatureCenter` 的功能按使用频次做“高频胶囊 + 进阶工具箱”分类，全部保留 `GoRouter` 目标页面跳转能力。

---

## 四、组件预览（Widget Previewer）支持

为了便于在 IDE 和浏览器中实时调试与验证 UI 效果，所有新建的 Elite 组件均遵循独立模块化标准：
* 编写无缝预览函数（或 `@Preview` 适配层）；
* 支持在 Light / Dark 模式及不同尺寸下即时微调间距与多巴胺色彩搭配。
