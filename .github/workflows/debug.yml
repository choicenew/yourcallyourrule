# .github/workflows/2-public-distribution-the-right-way.yml

name: 2. Sync Release (The Right Way)

on:
  release:
    types: [published]
  workflow_dispatch:

jobs:
  sync-release:
    name: Sync Release Using Proven Methods
    runs-on: ubuntu-latest
    
    steps:
      # --- 步骤 1: 强制 gh CLI 使用您的 PAT 登录 ---
      # 这是我们已经证明成功的、最关键的一步
      - name: Authenticate gh CLI with PAT
        run: echo "${{ secrets.MY_PAT }}" | gh auth login --with-token

      # --- 步骤 2: 确定要操作的确切 Release 标签 ---
      - name: Get Exact Target Tag
        id: get_tag
        run: |
          if [[ "${{ github.event_name }}" == "release" ]]; then
            # 如果由 release 事件触发，直接使用事件中的确切标签，这是最可靠的方式
            TARGET_TAG="${{ github.event.release.tag_name }}"
            echo "Triggered by release event. Using exact tag: $TARGET_TAG"
          else
            # 如果是手动触发，我们才去获取列表中的第一个（也就是最新的那个，无论是不是预发布）
            echo "Manual trigger. Fetching the most recent tag from private repo..."
            TARGET_TAG=$(gh release list --repo ${{ github.repository }} --limit 1 --json tagName --jq '.[0].tagName')
          fi
          
          if [[ -z "$TARGET_TAG" ]]; then
            echo "::error::Could not determine a target release tag."
            exit 1
          fi
          
          echo "tag=${TARGET_TAG}" >> $GITHUB_OUTPUT

      # --- 步骤 3: 获取、下载并发布 ---
      # 将所有操作放在一个脚本中，使用我们已验证成功的认证状态
      - name: Fetch, Download, and Sync Release
        env:
          TARGET_REPO: "choicenew/yourcallrule"
          SOURCE_TAG: ${{ steps.get_tag.outputs.tag }}
        run: |
          set -e

          # --- A. 使用【原始标签】从【私有库】获取标题和内容 ---
          echo "Fetching release details from private repo using tag: $SOURCE_TAG"
          RELEASE_DETAILS_JSON=$(gh release view "$SOURCE_TAG" --repo ${{ github.repository }} --json name,body)
          TITLE=$(echo "$RELEASE_DETAILS_JSON" | jq -r '.name')
          BODY=$(echo "$RELEASE_DETAILS_JSON" | jq -r '.body')

          # --- B. 使用【原始标签】从【私有库】下载所有附件 ---
          echo "Downloading assets from private repo using tag: $SOURCE_TAG"
          mkdir -p release_assets
          gh release download "$SOURCE_TAG" --repo ${{ github.repository }} --dir release_assets
          
          # --- C. 创建一个用于公开仓库的安全标签 ---
          SAFE_TARGET_TAG="${SOURCE_TAG//+/-}"
          echo "Using safe target tag for public repo: $SAFE_TARGET_TAG"

          # --- D. 在公开仓库创建或更新 Release ---
          echo "Creating or updating release '$SAFE_TARGET_TAG' in $TARGET_REPO..."
          cd release_assets
          
          # 先尝试删除已存在的 release，确保每次都是全新的创建
          gh release delete "$SAFE_TARGET_TAG" --repo "$TARGET_REPO" --yes || echo "Release '$SAFE_TARGET_TAG' did not exist. Proceeding to create."

          # 使用最基本的 gh release create 命令
          gh release create "$SAFE_TARGET_TAG" \
            --repo "$TARGET_REPO" \
            --title "$TITLE" \
            --notes "$BODY" \
            * # 上传当前目录下的所有文件

          echo "✅ Successfully synced release to public repo."
