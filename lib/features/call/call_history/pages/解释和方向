核心问题诊断
当前代码主要存在三大问题，它们互为因果，导致了您看到的所有混乱：
UI 重复与不一致: CallTimelineView 自己实现了一套卡片样式 (_buildCallCard)，而 CallLogsList 又使用了另一套 (CallLogCard.dart)。这是万恶之源，导致了样式不统一、一个地方有 bug 另一个地方没有（比如SIM卡信息）。
逻辑与视图耦合: 所有的操作逻辑（如何拨打电话、如何拦截、如何弹窗）都直接写在了 UI 组件（CallTimelineView）内部。这让 UI 组件变得臃肿，既要负责“长什么样”，又要负责“做什么事”，难以维护。
渲染架构错误: 主页面 (CallHistoryPage...) 在“列表模式”下，它的 itemBuilder 本应只构建一个小小的卡片，但它却错误地去构建了一个完整的 CallTimelineView 或 CallLogsList。这就像在一个抽屉里又装了一个完整的衣柜，是灾难性的性能问题和结构错误。
核心修改思路 (三步走战略)
第一步：抽象与统一UI组件 -> 打造唯一的“标准卡片”
目标: 解决“UI重复与不一致”的问题。
思路:
我们要确立 CallLogCard.dart 这个文件作为我们项目中显示“单条通话记录”的唯一标准组件。它应该是“唯一真神”。
我们将 CallTimelineView 中那个更好看的卡片样式（包括图标、布局、标签样式等）**“移植”**到 CallLogCard.dart 中，并修复所有已知 bug（如空SIM卡信息问题）。
完成这一步后，CallLogCard.dart 将成为一个功能完整、样式精美、无 bug 的独立组件。
之后，无论是时间轴视图 (CallTimelineView) 还是普通列表视图 (CallLogsList)，当需要显示一条通话记录时，它们都必须、也只能使用这个 <CallLogCard> 组件。绝不允许再有任何自定义的卡片实现。
第二步：分离业务逻辑 -> 创造一个专职的“处理器”
目标: 解决“逻辑与视图耦合”的问题。
思路:
我们要创建一个全新的、纯粹的 Dart 类，命名为 CallHistoryActionHandler。这个类不是 Widget，它是一个**“工具人”**或“处理器”。
它的职责非常专一：接收一条通话记录 (CallLog)，并负责处理所有与它相关的用户操作。例如，handleCallAction() 方法被调用时，它内部会判断是该弹“拨打/拦截”窗，还是该弹“解除拦截”窗，并执行后续的服务调用和UI反馈（如 SnackBar）。
在第一步创建的 CallLogCard 中，当用户点击某个操作按钮（比如电话图标）时，它不再自己写 if/else 逻辑。而是简单地创建一个 CallHistoryActionHandler 的实例，然后调用 handler.handleCallAction()。它把“脏活累活”全都交给了这个处理器。
这样就实现了职责分离：CallLogCard 只管貌美如花（UI渲染），CallHistoryActionHandler 只管干活养家（业务逻辑）。
第三步：重构页面渲染架构 -> 明确“导演”与“演员”
目标: 解决“渲染架构错误”的问题。
思路:
主页面 (CallHistoryPage...) 就是**“总导演”**。它有一个状态 _showTimelineView。
导演的工作是在 build 方法的最顶层就做出决定：今天拍的是“时间轴场景”还是“列表场景”？
如果 _showTimelineView 为 true，导演就直接构建一个完整的 <CallTimelineView> 组件，并把所有筛选后的通话记录 List<CallLog> 一次性传给它。
如果 _showTimelineView 为 false，导演就直接构建一个完整的 <CallLogsList> 组件（或者直接是一个 ListView），然后让它去渲染 List<CallLog>。
最关键的是：必须彻底删除主页面中那个错误的 _buildCallLogItem 方法。因为导演不应该去教每个“演员”（itemBuilder）该演哪个场景，这是结构混乱的根源。
总结
按照这个思路，我们的修改路径非常清晰：
统一UI：将所有卡片样式统一到 CallLogCard.dart。
分离逻辑：将卡片上的操作逻辑剥离到 CallHistoryActionHandler.dart。
修正架构：让主页面在顶层决定渲染哪个整体视图，而不是在底层 itemBuilder 里做判断。
这个思路能从根本上解决您遇到的所有问题，让代码变得清晰、一致、高效、且易于维护。