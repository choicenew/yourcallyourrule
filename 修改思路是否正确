好的，您意识到一个问题：在 _performSync() 函数中，pendingOperations 和 remoteChanges 包含了所有可能的操作类型（包括 PROPOSE_DELETION 和 DELETION_PROPOSAL_INFO），但是您的代码目前只把它们传递给了 _dataSource (也就是 RemoteNumberDataSource)。

而我们刚刚确认了：

Push 方面: _dataSource.getLocalPendingOperations() 是正确的，因为它直接从 pending_operations 表读取所有类型的操作。这部分没有问题，所有操作都会被 Push 到云端。

Pull 方面: _dataSource.applyRemoteChanges(remoteChanges) 是有问题的，因为它目前的实现只处理了 remote_numbers 相关的增删改查。

问题点： DELETION_PROPOSAL_INFO 这种类型的变更，目前被 _dataSource 默默地丢弃了。

如何在 Flutter 端修复这个问题？

如我之前所说，最务实的方法是在 RemoteNumberDataSource 中添加处理逻辑。因为 RemoteNumberDataSource 持有 RemoteDatabase 实例，它完全有能力去操作 active_deletion_proposals 表。

步骤 1：修改 RemoteNumberDataSource 的 applyRemoteChanges 方法

打开 lib/features/datasources/remote/remote_number_datasource.dart，找到 applyRemoteChanges 方法，并添加一个新的 case。

code
Dart
download
content_copy
expand_less
// remote_number_datasource.dart

  Future<void> applyRemoteChanges(List<Map<String, dynamic>> changes) async {
    await _database.transaction(() async {
      for (final change in changes) {
        final operation = change['operation'] as String;
        final entityId = change['entityId'] as String;
        final payload = change['payload'] as Map<String, dynamic>?;

        switch (operation) {
          case 'INSERT':
          case 'UPDATE':
            // ... (保持不变)
            break;
          case 'DELETE':
            // ... (保持不变)
            break;
          case 'INCREMENT':
            // ... (保持不变)
            break;

          // =================================================================
          // --- ✅ 新增：处理删除提议的同步信息 ---
          // =================================================================
          case 'DELETION_PROPOSAL_INFO':
            if (payload == null) continue;

            // 将 payload 映射到 Drift 的 Companion 对象
            // 注意：这里需要处理数据类型转换，确保安全
            final companion = ActiveDeletionProposalsCompanion(
              phoneNumber: Value(entityId),
              proposalStartTime: Value(payload['proposalStartTime']?.toString() ?? DateTime.now().toIso8601String()),
              status: const Value('pending'), // 从服务器拉取的活跃提议总是 pending
              highestRiskLevel: Value(payload['highestRiskLevel']?.toString() ?? 'Unknown'),
              proposalCount: Value((payload['proposalCount'] as num?)?.toInt() ?? 0),
              verifiedOwnerCount: Value((payload['verifiedOwnerCount'] as num?)?.toInt() ?? 0),
              lastUpdated: Value(DateTime.now().toIso8601String()), // 本地更新时间
              verificationReportJson: Value(payload['verificationReportJson']?.toString()),
            );

            // 执行插入或替换
            await _database.into(_database.activeDeletionProposals).insert(
              companion,
              mode: InsertMode.insertOrReplace,
            );
            break;
          // =================================================================

        }
      }
    });
  }
步骤 2：确保 Worker 返回了 proposalStartTime

这非常关键。正如我们在之前的分析中发现的，您的 worker.js 的 handlePull 函数中，构建 DELETION_PROPOSAL_INFO 的 payload 时，漏掉了 proposalStartTime。

请务必修改 worker.js，加上这一行：

code
JavaScript
download
content_copy
expand_less
// worker.js -> handlePull -> finalOperations.push

payload: {
  // ...
  proposalStartTime: proposal.proposal_start_time, // <--- 必须加上这一行
  // ...
}

(如果您已经使用了我之前提供的最新版 worker.js，请检查一下这里是否包含。如果不包含，请加上。)

总结

一旦您完成了这两步修改：

Worker 端: handlePull 会在返回的 JSON 中包含 proposalStartTime。

App 端: RemoteNumberDataSource 会识别 DELETION_PROPOSAL_INFO 操作，并把数据正确地写入到本地的 active_deletion_proposals 表中。

这样，从 App A 提议 -> 到 Cloudflare -> 到 App B 同步 的整个数据闭环就彻底打通了。App B 的用户就能看到 App A 提交的提议了。