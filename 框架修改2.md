lib/
  ├── core/                      # 核心功能
  │   ├── platform/             # 平台通道
  │   ├── utils/                # 工具类
  │   ├── constants/            # 常量
  │   └── managers/             # 全局管理器
  │       ├── ad_manager/       # 广告管理
  │       ├── auth_manager/     # 认证管理
  │       └── permission_manager/# 权限管理
  │
  ├── data/                     # 数据层
  │   ├── datasources/          # 数据源
  │   │   ├── local/           # 本地数据源
  │   │   │   ├── database/    # 数据库
  │   │   │   └── preferences/ # 本地存储
  │   │   └── remote/          # 远程数据源
  │   │       ├── api/         # API接口
  │   │       └── cloud/       # 云服务
  │   ├── models/              # 数据模型
  │   │   ├── call/           # 通话相关模型
  │   │   ├── sms/            # 短信相关模型
  │   │   └── user/           # 用户相关模型
  │   └── repositories/        # 仓库实现
  │
  ├── domain/                   # 领域层
  │   ├── entities/            # 业务实体
  │   │   ├── call/           # 通话实体
  │   │   ├── sms/            # 短信实体
  │   │   └── user/           # 用户实体
  │   └── usecases/            # 用例
  │
  ├── presentation/            # 表现层
  │   ├── providers/          # 状态管理
  │   │   ├── call/          # 通话状态
  │   │   ├── sms/           # 短信状态
  │   │   └── app/           # 应用状态
  │   ├── screens/           # 页面
  │   └── widgets/           # 可复用组件
  │       ├── common/        # 通用组件
  │       ├── call/          # 通话组件
  │       └── sms/           # 短信组件
  │
  ├── services/               # 服务层
  │   ├── base/              # 基础服务类
  │   │   ├── base_service.dart
  │   │   └── base_db_service.dart
  │   ├── call/              # 通话相关服务
  │   │   ├── call_filter.dart
  │   │   └── call_monitor.dart
  │   ├── sms/               # 短信相关服务
  │   │   ├── sms_filter.dart
  │   │   └── sms_monitor.dart
  │   └── sync/              # 同步备份服务
  │
  ├── features/              # 功能模块
  │   ├── ads/              # 广告功能
  │   │   ├── models/      # 广告模型
  │   │   ├── services/    # 广告服务
  │   │   └── widgets/     # 广告组件
  │   ├── auth/            # 认证功能
  │   └── settings/        # 设置功能
  │
  └── main.dart             # 应用入口